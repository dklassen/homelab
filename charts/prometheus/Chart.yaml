apiVersion: v2
name: prometheus
description: Prometheus wrapper chart — bundles upstream prometheus with ClickHouse scrape config
type: application
version: 0.1.0
dependencies:
  - name: prometheus
    version: "29.9.0"
    repository: https://prometheus-community.github.io/helm-charts
